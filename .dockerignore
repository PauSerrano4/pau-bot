venv/
__pycache__/
*.pyc
.git/
.gitignore
chats/
*.zip

# Extracted personal data. If you want it inside the image (for example, to
# deploy with your style already indexed), remove the following lines.
# If you prefer each person to generate their own corpus, leave them in place.
all_extracted_examples.py
filtered_examples.py
style_index.pkl